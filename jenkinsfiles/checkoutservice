pipeline {
    agent any 

    stages {
        stage('Cleaning Workspace') {
            steps {
                cleanWs()
            }
        }
        stage('Checkout from Git') {
            steps {
                git branch: 'main', url: 'https://github.com/Ahmedhessn/E-Commerce-Application-Microservice.git'
            }
        }
        stage("Docker Image Build") {
            steps {
                script {
                    dir('src/cartservice/src') {
                        sh 'docker build -t cartservice .'
                    }
                }
            }
        }
        stage("ECR Image Pushing") {
            steps {
                script {
                    sh '''
                    aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 503459125797.dkr.ecr.us-east-1.amazonaws.com
                    docker tag cartservice:latest 503459125797.dkr.ecr.us-east-1.amazonaws.com/cartservice:${BUILD_NUMBER}
                    docker push 503459125797.dkr.ecr.us-east-1.amazonaws.com/cartservice:${BUILD_NUMBER}
                    '''
                }
            }
        }
        stage('Update Deployment file') {
            environment {
                GIT_REPO_NAME = "E-Commerce-Application-Microservice"
                GIT_EMAIL = "ahmed.hessn1212@gmail.com"
                GIT_USER_NAME = "Ahmedhessn"
                IMAGE_NAME = "cartservice"
                REPO_URL = "503459125797.dkr.ecr.us-east-1.amazonaws.com/cartservice"
                YAML_FILE = "cartservice.yaml"
            }
            steps {
                dir('kubernetes-files') {
                    withCredentials([string(credentialsId: 'my-git-pattoken', variable: 'git_token')]) {
                        sh """
                            git config user.email "${GIT_EMAIL}"
                            git config user.name "${GIT_USER_NAME}"
                            
                            sed -i "s#image:.*#image: ${REPO_URL}:${BUILD_NUMBER}#g" ${YAML_FILE}
                            
                            git add .
                            
                            # الـ if دي بتحميك لو مفيش تغييرات حقيقية في الملف
                            if git commit -m "Update ${IMAGE_NAME} Image to version ${BUILD_NUMBER}"; then
                                # السلاش قبل git_token دي عشان الـ Security
                                git push https://\${git_token}@github.com/Ahmedhessn/${GIT_REPO_NAME}.git HEAD:main
                            else
                                echo "No changes to commit"
                            fi
                        """
                    }
                }
            }
        }
    }
}